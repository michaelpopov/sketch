CREATE CATALOG one;
LIST DATASETS one;
CREATE DATASET one.aaa TYPE=f16 DIM=256 COUNT=8;
LIST DATASETS one;
SHOW DATASET one.aaa;
CREATE DATASET one.aaa TYPE=f16 DIM=256 COUNT=8;
CREATE DATASET one.bbb TYPE=f32 DIM=256 COUNT=8;
LIST DATASETS one;
SHOW DATASET one.bbb;
DROP DATASET one.aaa;
LIST DATASETS one;

